---
// Clientes component for landing page
---

<section id="clientes" class="py-16 bg-white">
  <div class="container mx-auto px-4">
    <div class="text-center mb-12">
      <span
        class="text-2xl md:text-3xl font-bold inline-block bg-primary text-white px-4 py-1 rounded-md mb-4"
        >👥 Clientes destacados</span
      >
      <h2 class="text-3xl md:text-4xl font-bold font-heading mb-6">
        Confían en nosotros
      </h2>
      <p class="text-lg text-gray-700 max-w-3xl mx-auto">
        Hemos suministrado tableros a múltiples empresas y particulares a lo
        largo de nuestra trayectoria.
      </p>
    </div>

    <!-- Placeholder para logos de clientes -->
    <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8 mb-12">
      {
        Array(8)
          .fill(0)
          .map((_, i) => (
            <div class="bg-gray-100 rounded-lg p-6 flex items-center justify-center h-32">
              <div class="text-gray-400 text-center">
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  class="h-12 w-12 mx-auto mb-2"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="1.5"
                    d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"
                  />
                </svg>
                <p class="text-sm font-medium">Logo Cliente {i + 1}</p>
              </div>
            </div>
          ))
      }
    </div>

    <div class="bg-primary bg-opacity-10 p-6 rounded-lg text-center">
      <p class="text-lg font-semibold text-primary">
        ¿Quieres ser parte de nuestra lista de clientes satisfechos? <a
          href="#contacto"
          class="underline hover:text-accent transition-colors duration-300"
          >Contáctanos hoy mismo</a
        >.
      </p>
    </div>
  </div>
</section>
